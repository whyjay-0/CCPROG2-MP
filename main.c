#include "Letran_Navarrosa_Machine-Project.h"

int main () {
	
}

/* // registerUser function test
	
	int userCount=0; //sample user count maybe final?
	printf("Account created successfully. User ID: %02d", registerUser(&userCount));
	printf("Account created successfully. User ID: %02d", registerUser(&userCount));
*/
	
/* // hashPassword function test
	
	char *password = "mypassword";  // sample
    unsigned long passwordHash; // storing hash
    hashPassword(password, &passwordHash);
    printf("Hashed Password: %lu\n", passwordHash);
*/
